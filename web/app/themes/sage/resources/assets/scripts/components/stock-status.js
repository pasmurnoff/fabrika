export default function stockStatus() {
 /*   function unavailable() {
        return $('.product__add-cart').hasClass('wc-variation-is-unavailable');
    }

    function needed() {
        return $('.product__add-cart').hasClass('wc-variation-selection-needed');
    }

    function stockMessage() {
        const $inStock = $('.single-product__stock-wrap');
        (unavailable() || needed()) ? $inStock.removeClass('shown') : $inStock.addClass('shown');
    }

    $(document).ready(function () {
        stockMessage();
        $('body').on('click', '.swatch_wrapper', function () {
            stockMessage();
        })
    })*/
}