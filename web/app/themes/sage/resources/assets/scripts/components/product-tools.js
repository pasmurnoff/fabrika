/*
/!*console.log($('.product__tools').parent().outerHeight());*!/

let heights = [];  // переменная, которая будет хранить высоты элементов

$('.columns-6 li.product, .columns-4 li.product').each(function (indx, element) {
    heights.push($(element).find('img').css('height'));
});
console.log(heights);*/
